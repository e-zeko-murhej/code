import { createMuiTheme } from '@material-ui/core/styles';

const theme = {
  palette: {
    primary: {
      main: '#5126aa',
    },
    secondary: {
      main: '#ffffff',
    },
    text: {
      primary: '#212529',
      secondary: '#ffffff',
    },
  },
};

export default createMuiTheme(theme);
